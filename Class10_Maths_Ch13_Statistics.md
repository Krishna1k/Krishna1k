# Class 10 NCERT Maths — Chapter 13: Statistics

> A simplified, detailed guide in Hinglish for students who missed Class 7-8-9.
> Based on the official NCERT (English Medium) textbook.

---

## SECTION 1: Foundation

### 1.1 Statistics kya hai?

**Statistics** = data ko collect, organize, aur analyse karne ki vidya. Is chapter mein hum **grouped data** (intervals mein bante data) ka **central tendency** nikaalte hain.

### 1.2 Teen Central Tendency Measures

| Measure | Matlab |
|---|---|
| **Mean (औसत)** | Average — sab values ka total ÷ count |
| **Median** | Beech wali value (sort karne ke baad) |
| **Mode** | Sabse zyada baar aane wali value |

### 1.3 Grouped Data ke terms

Maan le data aise diya hai:

| Class Interval | Frequency (fᵢ) |
|---|---|
| 0-10 | 5 |
| 10-20 | 8 |
| 20-30 | 12 |

- **Class Interval:** range (jaise 0-10)
- **Frequency (fᵢ):** us interval mein kitne items hain
- **Class Mark (xᵢ):** interval ka midpoint = (upper + lower)/2
  - Jaise 0-10 ka class mark = (0+10)/2 = 5

---

## SECTION 2: MEAN of Grouped Data (3 methods)

### 2.1 Method 1: Direct Method

```
Mean (x̄) = Σ(fᵢxᵢ) / Σfᵢ
```

**Steps:**
1. Har interval ka **class mark xᵢ** nikaalo
2. **fᵢ × xᵢ** nikaalo har row ke liye
3. Σ(fᵢxᵢ) aur Σfᵢ ka sum karo
4. Divide karo

#### Example 1:

| CI | fᵢ | xᵢ (mark) | fᵢxᵢ |
|---|---|---|---|
| 0-10 | 5 | 5 | 25 |
| 10-20 | 8 | 15 | 120 |
| 20-30 | 12 | 25 | 300 |
| 30-40 | 5 | 35 | 175 |
| **Total** | **30** | | **620** |

```
Mean = Σfᵢxᵢ / Σfᵢ = 620 / 30 = 20.67
```

### 2.2 Method 2: Assumed Mean Method

Jab numbers bade ho, to ek **assumed mean `a`** le lo (usually beech wala class mark), phir deviation `dᵢ = xᵢ - a`.

```
Mean = a + (Σfᵢdᵢ / Σfᵢ)
```

### 2.3 Method 3: Step Deviation Method

Jab class size `h` saman ho:

```
uᵢ = (xᵢ - a) / h

Mean = a + h × (Σfᵢuᵢ / Σfᵢ)
```

(Calculation aur easy ho jaati hai — chhote numbers)

---

## SECTION 3: MODE of Grouped Data

### 3.1 Modal Class

**Modal class** = sabse zyada frequency wala interval.

### 3.2 Formula

```
Mode = l + [ (f₁ - f₀) / (2f₁ - f₀ - f₂) ] × h
```

Yahan:
- `l` = modal class ka lower limit
- `f₁` = modal class ki frequency
- `f₀` = modal class se **pehle** wali frequency
- `f₂` = modal class ke **baad** wali frequency
- `h` = class size

#### Example 2:

| CI | Frequency |
|---|---|
| 0-10 | 3 |
| 10-20 | 8 ← f₀ |
| **20-30** | **12 ← f₁ (modal class)** |
| 30-40 | 7 ← f₂ |

```
l = 20, f₁ = 12, f₀ = 8, f₂ = 7, h = 10

Mode = 20 + [(12-8)/(2×12 - 8 - 7)] × 10
     = 20 + [4/(24-15)] × 10
     = 20 + (4/9) × 10
     = 20 + 4.44
     = 24.44
```

---

## SECTION 4: MEDIAN of Grouped Data

### 4.1 Median Class

Pehle **cumulative frequency (cf)** nikaalo (frequencies ko add karte jao). Phir n/2 dekho — jis class mein n/2 aata hai wahi **median class**.

### 4.2 Formula

```
Median = l + [ (n/2 - cf) / f ] × h
```

Yahan:
- `l` = median class ka lower limit
- `n` = total frequency (Σfᵢ)
- `cf` = median class se **pehle** wali cumulative frequency
- `f` = median class ki frequency
- `h` = class size

#### Example 3:

| CI | f | cf |
|---|---|---|
| 0-10 | 5 | 5 |
| 10-20 | 8 | 13 |
| **20-30** | **12** | **25** ← median class |
| 30-40 | 5 | 30 |

```
n = 30, so n/2 = 15
n/2 = 15 first crosses at cf = 25 → median class = 20-30
l = 20, cf (before) = 13, f = 12, h = 10

Median = 20 + [(15 - 13)/12] × 10
       = 20 + (2/12) × 10
       = 20 + 1.67
       = 21.67
```

---

## SECTION 5: Empirical Relationship

In teeno ke beech ek relation hota hai (approximate):

```
3 Median = Mode + 2 Mean
```

(Ya: Mode = 3 Median − 2 Mean). Ek nikaalna ho aur do pata ho toh ye use kar sakte ho.

---

## SECTION 6: Q&A TIME

**Q1.** Class interval 30-40 ka class mark (xᵢ) kya hoga?

**Q2.** Mean nikaal (direct method):

| CI | fᵢ |
|---|---|
| 0-10 | 4 |
| 10-20 | 6 |
| 20-30 | 10 |

**Q3.** Mode formula likho aur batao f₀, f₁, f₂ kya hote hain.

**Q4.** Median formula mein `cf` ka kya matlab hai?

**Q5.** Agar Mean = 30 aur Median = 28, toh empirical relation se Mode nikaal.

**Q6.** Modal class aur median class mein kya difference hai?

---

## SUMMARY

1. **Class mark** xᵢ = (upper + lower)/2

2. **Mean (Direct):** `x̄ = Σfᵢxᵢ / Σfᵢ`

3. **Mean (Step deviation):** `x̄ = a + h(Σfᵢuᵢ/Σfᵢ)`, uᵢ = (xᵢ-a)/h

4. **Mode:** `l + [(f₁-f₀)/(2f₁-f₀-f₂)] × h`

5. **Median:** `l + [(n/2 - cf)/f] × h`

6. **Empirical:** `3 Median = Mode + 2 Mean`

---

Generated by Kiro for Krishna1k
