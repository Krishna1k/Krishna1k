# Class 10 NCERT Maths — Chapter 7: Coordinate Geometry

> A simplified, detailed guide in Hinglish for students who missed Class 7-8-9.
> Based on the official NCERT (English Medium) textbook.

---

## SECTION 1: Foundation

### 1.1 Coordinate Geometry kya hai?

**Coordinate Geometry** = geometry ko numbers/algebra se jodne ka tareeka.
Points ko numbers (coordinates) se represent karte hain aur graph paper pe plot karte hain.

### 1.2 Cartesian Plane (recap)

Do lines milti hain:
- **X-axis** → horizontal (leti hui)
- **Y-axis** → vertical (khadi)
- Meeting point = **Origin (0, 0)**

```
              Y-axis
                |
        Q2      |      Q1
    (-x, +y)    |   (+x, +y)
                |
   -------------O-------------- X-axis
                |
        Q3      |      Q4
    (-x, -y)    |   (+x, -y)
                |
```

### 1.3 Coordinates of a Point

Har point ko `(x, y)` se likhte hain:
- **x = abscissa** → X-axis pe distance (right +ve, left -ve)
- **y = ordinate** → Y-axis pe distance (up +ve, down -ve)

⚠️ **Order matters!** `(3, 5)` aur `(5, 3)` alag points hain.

### 1.4 Four Quadrants

| Quadrant | x sign | y sign | Example |
|---|---|---|---|
| Q1 | + | + | (3, 4) |
| Q2 | - | + | (-3, 4) |
| Q3 | - | - | (-3, -4) |
| Q4 | + | - | (3, -4) |

**Special points:**
- Origin = (0, 0)
- On X-axis → y = 0, jaise (5, 0)
- On Y-axis → x = 0, jaise (0, 7)

---

## SECTION 2: Distance Formula

### 2.1 The Formula (Important!)

Do points `A(x₁, y₁)` aur `B(x₂, y₂)` ke beech ka distance:

```
AB = √[(x₂ - x₁)² + (y₂ - y₁)²]
```

**Kahan se aaya?** Ye Pythagoras theorem se banaa hai! (x difference)² + (y difference)² = (distance)².

### 2.2 Distance from Origin

Point `P(x, y)` ka origin `(0, 0)` se distance:

```
OP = √(x² + y²)
```

### 2.3 Examples

#### Example 1: A(2, 3) aur B(5, 7) ke beech distance nikaal

```
AB = √[(5-2)² + (7-3)²]
   = √[(3)² + (4)²]
   = √[9 + 16]
   = √25
   = 5
```

**Distance = 5 units** ✅

#### Example 2: P(-4, 3) ka origin se distance

```
OP = √[(-4)² + (3)²]
   = √[16 + 9]
   = √25
   = 5
```

**Distance = 5 units** ✅

#### Example 3: Check karo — kya A(1, 5), B(2, 3), C(-2, -11) collinear hain?

(Collinear = ek hi line pe). Check karo: agar AB + BC = AC ho toh collinear.

```
AB = √[(2-1)² + (3-5)²] = √[1 + 4] = √5
BC = √[(-2-2)² + (-11-3)²] = √[16 + 196] = √212
AC = √[(-2-1)² + (-11-5)²] = √[9 + 256] = √265
```

AB + BC ≠ AC → **Not collinear**.

---

## SECTION 3: Section Formula

### 3.1 Kya hai?

Agar koi point P, line segment AB ko **m₁ : m₂ ratio** mein **internally divide** karta hai, toh P ke coordinates:

```
P(x, y) = ( (m₁x₂ + m₂x₁)/(m₁ + m₂) , (m₁y₂ + m₂y₁)/(m₁ + m₂) )
```

jahan A = (x₁, y₁), B = (x₂, y₂).

### 3.2 Example

#### Example 4: A(4, -1) aur B(-2, 3) ko 2:1 ratio mein divide karne wala point nikaal

m₁ = 2, m₂ = 1, A(4, -1), B(-2, 3)

```
x = (2(-2) + 1(4))/(2+1) = (-4 + 4)/3 = 0/3 = 0
y = (2(3) + 1(-1))/(2+1) = (6 - 1)/3 = 5/3
```

**Point = (0, 5/3)** ✅

---

## SECTION 4: Midpoint Formula

### 4.1 The Formula

**Midpoint** = jab koi point segment ko **exactly beech mein (1:1)** divide kare.

```
Midpoint M = ( (x₁ + x₂)/2 , (y₁ + y₂)/2 )
```

(Bas dono x ka average, dono y ka average!)

### 4.2 Examples

#### Example 5: A(4, 6) aur B(8, 10) ka midpoint nikaal

```
M = ( (4+8)/2 , (6+10)/2 )
  = ( 12/2 , 16/2 )
  = (6, 8)
```

**Midpoint = (6, 8)** ✅

#### Example 6: Agar midpoint (3, 4) hai aur ek end A(1, 2) hai, doosra end B nikaal

Let B = (x, y).
```
(1+x)/2 = 3 → 1+x = 6 → x = 5
(2+y)/2 = 4 → 2+y = 8 → y = 6
```

**B = (5, 6)** ✅

---

## SECTION 5: Q&A TIME

**Q1.** Inn points ka quadrant bata:
- (a) (5, -3)
- (b) (-2, -7)
- (c) (4, 8)
- (d) (-1, 6)

**Q2.** A(0, 0) aur B(6, 8) ke beech distance nikaal.

**Q3.** P(3, 4) ka origin se distance kya hai?

**Q4.** A(-1, 2) aur B(5, 8) ka midpoint nikaal.

**Q5.** A(1, 2) aur B(7, 8) ko 1:2 ratio mein divide karne wala point nikaal (section formula).

**Q6.** Check karo: kya A(0, 0), B(3, 4) ke beech distance 5 hai? (Pythagorean triplet use kar)

---

## SUMMARY

1. **Point:** `(x, y)` → x = abscissa, y = ordinate. Order matters!

2. **Quadrants:** Q1(+,+), Q2(-,+), Q3(-,-), Q4(+,-)

3. **Distance Formula:**
   ```
   AB = √[(x₂-x₁)² + (y₂-y₁)²]
   ```

4. **Distance from origin:** `√(x² + y²)`

5. **Section Formula** (m₁:m₂ ratio):
   ```
   ( (m₁x₂+m₂x₁)/(m₁+m₂) , (m₁y₂+m₂y₁)/(m₁+m₂) )
   ```

6. **Midpoint Formula:**
   ```
   ( (x₁+x₂)/2 , (y₁+y₂)/2 )
   ```

---

**Note:** Latest NCERT (2023-24) syllabus mein "Area of Triangle" wala topic Coordinate Geometry se hata diya gaya hai, isliye yahan include nahi kiya.

---

Generated by Kiro for Krishna1k
